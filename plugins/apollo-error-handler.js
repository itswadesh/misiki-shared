export default (e, {store}) => {
    // store.commit('setErr',e,{root:true})
    // const { graphQLErrors, networkError, operation, forward} = e
    // console.error(graphQLErrors, networkError, operation, forward)
}